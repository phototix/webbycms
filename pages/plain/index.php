<div class="container my-5">
    <h1>Plain static page</h1>
    <p>This page has no forms and no session usage, so it is served straight from the anonymous page cache
        (when <code>PAGE_CACHE_ENABLED=true</code>).</p>
</div>
